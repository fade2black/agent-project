networks:
  agent-net:
    driver: bridge

services:
  agent:
    build:
      context: ../..
      dockerfile: bin/discovery_demo/Dockerfile
      target: final
    environment:
      DISCOVERY_PORT: 4000
      DISCOVERY_INTERVAL: 10
      AGENT_TTL: 30
      AGENT_CLEANUP_INTERVAL: 5
      HTTP_PORT: 8080
    networks:
      - agent-net
